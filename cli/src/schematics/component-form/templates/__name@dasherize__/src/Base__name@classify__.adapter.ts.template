export interface <%= classify(name) %>Adapter {
  // eslint-disable-next-line @typescript-eslint/ban-types
  // 以下是 vant3 Attrs 和 events
  // @see https://vant-contrib.gitee.io/vant/v3/#/zh-CN/<%= dasherize(name) %>
  on?: {}
}
